{
  "private": true,
  "scripts": {
    "start": "npm run build && npm run serve",
    "serve": "node dist/server/app",
    "build": "../../prettynice/scripts/build.sh"
  },
  "dependencies": {
    "gren-lang": "^0.3.0"
  }
}
